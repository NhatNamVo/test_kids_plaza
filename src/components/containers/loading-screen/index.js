import style from "./style.module.scss";

const LoadingScreen = () => {
  return (
    <div className={style.loadingScreen}>
      <h1 className={style.branch}>The Starting Point of Success</h1>

      <div className={style.loadingIcon}>
        ...
      </div>
    </div>
  );
};

export default LoadingScreen;
